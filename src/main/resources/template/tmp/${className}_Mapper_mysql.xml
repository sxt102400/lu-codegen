<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<#include "func.ftl">
<mapper namespace="${mapperPackage}.${className}Mapper">
    <resultMap id="${className}Map" type="${entityPackage}.${className}">
<@resultMapList/>
    </resultMap>
    <sql id="Base_Column_List">
<@columnList/>
    </sql>

    <select id="countByExample" parameterType="${example}" resultType="java.lang.Integer">
        select count(1) from ${introspectedTable.tableName}
        <if test="_parameter != null">
            <include refid="common.Example_Where_Clause" />
        </if>
    </select>

    <delete id="deleteByExample" parameterType="${example}">
        delete from ${introspectedTable.tableName}
        <if test="_parameter != null" >
            <include refid="common.Example_Where_Clause" />
        </if>
    </delete>

<#if introspectedTable.hasUniquePk>
    <delete id="deleteByPrimaryKey" parameterType="<@pkType/>" >
        delete from ${introspectedTable.tableName}
        where <@pkCondition/>
    </delete>
</#if>


    <insert id="insert" parameterType="${entityPackage}.${className}">
        insert into ${introspectedTable.tableName} (
<@columnList/>
        ) values (
<@columnParamList/>
        )
    </insert>

    <insert id="insertSelective" parameterType="${entityPackage}.${className}">
        insert into ${introspectedTable.tableName}
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <@testColumnList/>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
           <@testColumnParameList/>
        </trim>
    </insert>


    <update id="updateByExample" parameterType="map">
        update ${introspectedTable.tableName}
        set
<@updateColumnListWithMap/>
        <if test="_parameter != null">
            <include refid="common.Update_By_Example_Where_Clause" />
        </if>
    </update>

    <update id="updateByExampleSelective" parameterType="map">
        update ${introspectedTable.tableName}
        <set>
<@testUpdateColumnListWithMap/>
        </set>
        <if test="_parameter != null">
            <include refid="common.Update_By_Example_Where_Clause" />
        </if>
    </update>

<#if introspectedTable.hasUniquePk>
    <update id="updateByPrimaryKey" parameterType="${entityPackage}.${className}">
        update ${introspectedTable.tableName}
        set
<@updateColumnList/>
        where <@pkCondition/>
    </update>

    <update id="updateByPrimaryKeySelective" parameterType="${entityPackage}.${className}">
        update ${introspectedTable.tableName}
        <set>
<@testUpdateColumnList/>
        </set>
        where <@pkCondition/>
    </update>

</#if>

    <select id="selectByExample" resultMap="${className}Map" parameterType="${example}">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List"/>
        from ${introspectedTable.tableName}
        <if test="_parameter != null" >
            <include refid="common.Example_Where_Clause" />
        </if>
        <if test="orderByClause != null" >
            order by ${r'${orderByClause}'}
        </if>
    </select>

<#if introspectedTable.hasUniquePk>
    <select id="selectByPrimaryKey" resultMap="${className}Map" parameterType="<@pkType/>">
        select
        <include refid="Base_Column_List" />
        from ${introspectedTable.tableName}
        where <@pkCondition/>
    </select>
</#if>

</mapper>